{
  "id": "casefold",
  "kind": "synthetic",
  "issue": "Normalize text by removing surrounding whitespace and applying Unicode case folding for caseless matching. Preserve internal whitespace.",
  "files": {
    "app.py": "def normalize(text):\n    return text.lower()\n"
  },
  "acceptance_tests": {
    "test_acceptance.py": "from app import normalize\ndef test_normalize():\n    assert normalize(\" Hello \") == \"hello\"\n    assert normalize(\"Stra\u00dfe\") == \"strasse\"\n"
  },
  "reference_files": {
    "app.py": "def normalize(text):\n    return text.strip().casefold()\n"
  }
}
