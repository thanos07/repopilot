{
  "id": "deduplicate",
  "kind": "synthetic",
  "issue": "Deduplicate values while preserving order.",
  "files": {
    "app.py": "def unique(values):\n    return sorted(set(values))\n"
  },
  "acceptance_tests": {
    "test_acceptance.py": "from app import unique\ndef test_unique():\n    assert unique([3, 1, 3, 2]) == [3, 1, 2]\n    assert unique([]) == []\n"
  },
  "reference_files": {
    "app.py": "def unique(values):\n    return list(dict.fromkeys(values))\n"
  }
}
